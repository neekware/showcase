{
  "name": "@show/source",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "__REPO_RELATED__": "____________________",
    "prepare": "husky install",
    "loc": "pnpm --filter infra loc",
    "update": "pnpm update --latest recursive",
    "clean": "pnpm recursive run clean",
    "clean:out": "rm recursivef ./tmp/coverage && rm recursivef .e2e",
    "clean:mod": "pnpm recursive run clean:mods",
    "reset:all": "pnpm clean && pnpm clean:out && pnpm clean:mod && pnpm i",
    "__FORMAT_RELATED__": "__________________",
    "format": "prettier -w \"**/*.{ts,tsx,js,jsx,html,css,svg,yml,json,md}\"",
    "lint": "pnpm recursive run lint",
    "__ACTION_RELATED__": "__________________",
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "__TEST_RELATED__": "__________________",
    "coverage": "find ./tmp/coverage -name lcov.info -exec cat {} + > ./tmp/coverage/lcov.combo.info",
    "test": "pnpm recursive run test",
    "e2e": "pnpm recursive run e2e",
    "e2e:report": "pnpm recursive run e2e:report"
  },
  "dependencies": {},
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "husky": "^9.0.11",
    "prettier": "^3.3.1",
    "prettier-plugin-tailwindcss": "^0.6.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.1.4"
}
